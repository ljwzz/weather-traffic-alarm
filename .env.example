# Local development credentials. Copy to .env and fill values locally.
# .env.local overrides .env. Missing or blank values clear the device field.
# All four values blank clears all device credentials on import.
AMAP_WEB_KEY=
AMAP_SDK_KEY=
CAIYUN_APP_KEY=
CAIYUN_APP_SECRET=
