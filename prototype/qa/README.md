# Web 原型验证记录

2026-09-03 通用 Android／小米权限引导、39 项提交快照测试及开发验收截图见 [权限原型验收](./permissions-2026-09-03/README.md)，33 个 Figma 场景与 109 条关键连接见 [设计校验](./permissions-2026-09-03/figma-qa.md)。

日期：2026-08-31。对象为 `prototype/`，不构成 Android 系统能力验收。

## 自动化检查

```bash
node --test prototype/tests/state.test.mjs
```

结果：14 项通过，覆盖默认未来单次 `06:00`、过去单次校验、每周至少一天、跨周、工作日跨周末、单日覆盖隔离、完成态、重复文案、铃声/振动/贪睡持久化、凭据排除、高德 fixture 状态和全局通勤／计划覆盖隔离。

服务测试需要绑定本机回环端口；受沙箱限制时应在允许本机监听的环境运行：

```bash
node --test prototype/tests/server.test.mjs
```

## 浏览器检查

使用 `http://127.0.0.1:4178` 和应用内浏览器完成：

| 场景 | 结果 |
|---|---|
| 首次空态 | 计划页显示“还没有闹钟”；首页显示本地闹钟空态。 |
| 创建 | 默认未来单次 `06:00`；切换为每周并保存后，列表展示选中日期和“待 Android 注册”。 |
| 编辑与单日覆盖 | 编辑草稿进入日历，当前月份显示；本日停用仅写入当前计划及日期。 |
| 启停与记录 | 停用后卡片状态更新，记录新增“已停用”事件。 |
| 高德 fixture | 首次授权、运行时 Web／SDK Key、无 Key、加载、定位拒绝、错误、地图选点、单次定位、五种路线、最多三条备选与当前路况均为离线 fixture；不发送请求。 |
| 控制台 | 上述关键流程未出现浏览器 error 日志。 |

截图：

- `local-alarm-plans.jpg`：已创建本地闹钟的计划页。
- `provider-not-connected.jpg`：天气服务未接入状态。

## 未覆盖

未在本原型中验证 Android 注册、到点响铃、前台服务、通知、锁屏、精确闹钟、重启恢复、设备权限、系统音量、真实节假日数据或第三方服务。上述项目应在 Android 36 模拟器和真机上验收。
