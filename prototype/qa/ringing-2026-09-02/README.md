# 基础／提前响铃同步验证

日期：2026-09-02。范围：Figma、规格、Web 原型及任务清单的响铃交互同步；本记录不是 Android 系统响铃或自动提前调度验收。

## 自动化

在仓库根目录执行：

```bash
node --test prototype/tests/*.test.mjs
git -c core.fsmonitor=false diff --check
```

结果：31 项通过、0 失败；差异空白检查通过。服务测试在允许本机回环监听的环境执行。

- `ringing-state.test.mjs`：基础／提前独立实例、停止与贪睡幂等、子实例、连续贪睡、跨日和 1–30 分钟校验。
- `screens-system.test.mjs`：基础与提前文案、停止／贪睡反馈、跨日星期、子实例提示和白色状态栏。
- `app-ringing.integration.test.mjs`：通过最小 DOM／存储存根执行真实 `app.js` 入口及点击处理器，检查路由、全屏外壳、停止、贪睡、重演、再次响铃、重进、重置和存储不变。
- `server.test.mjs`：两条响铃路由及依赖交付；原有状态、路线和服务检查一并通过。

## 浏览器实点

使用本轮启动的 `http://127.0.0.1:4173/` 与应用内浏览器，通过场景选择器和页面按钮验证。

| 检查 | 结果 |
|---|---|
| 基础初始 | `07:30`，基础原因卡，停止与贪睡入口。 |
| 提前初始 | `07:18`，提前 12 分钟及离线天气／通勤理由。 |
| 停止、重新演示 | 原路由显示已停止；重新演示回到初始时间。 |
| 基础贪睡 | `07:30 → 07:40 → 再次响铃 → 07:50`。 |
| 提前贪睡 | `07:18 → 07:28 → 再次响铃 → 07:38`。 |
| 子实例文案 | 显示“贪睡后再次响铃／贪睡提醒”，不沿用首次提前量。 |
| 返回与数据隔离 | 返回闹钟页仍为空列表；记录页仍为“暂无本机记录”。 |
| 控制台 | 本轮上述操作未出现 error／warn 日志。 |
| 视觉 | 独立 412 × 892 全屏；单套状态栏及底部手势；白色时间与按钮文案可见，无重叠或裁切。 |

浏览器截图：`basic.png`、`basic-stopped.png`、`basic-snoozed.png`、`basic-again.png`，以及对应的 `early*.png`。

## Figma

[本轮状态板](https://www.figma.com/design/wN04BlxRelbJyBVF35DyXE?node-id=95-600) 复用既有 `3:73` 的壁纸、布局、Material 按钮实例、颜色／间距变量及 Noto Sans SC／Roboto 字体。

- 八个状态：基础 `95:601`、停止 `95:655`、贪睡 `95:709`、子实例 `119:624`；提前 `95:763`、停止 `95:817`、贪睡 `95:871`、子实例 `119:678`。
- 原 `3:73` 已同步日期、演示边界及停止／贪睡连接。
- 首轮按钮连接已通过 API 回读核对；连接使用 Figma 点击反应，接口依据为 [Figma reactions 文档](https://developers.figma.com/docs/plugins/api/properties/nodes-reactions/)。
- `figma-state-audit.json` 保存本轮回读的节点、文案、尺寸和连接；全部八帧均为 412 × 892，字体检查无差异，均无占位状态。
- `figma-board.png` 为最终状态板截图；另已逐屏检查基础、提前、停止、贪睡及子实例视觉。
- Figma 连接覆盖首轮至首次子实例；子实例后的连续停止／贪睡与跨日由可运行的 Web 原型验证，不将静态设计稿当作无限状态模拟器。

## 边界

演示状态仅存在于当前页面会话，不播放音频或振动，不注册系统闹钟、不调用 Provider、不写入计划、记录或日期覆盖。Android 基础响铃的既有完成情况与剩余契约／验收项分别列于 `IMPLEMENTATION_TASKS.md`；本轮未运行 Android 构建或设备测试。自动提前仍未启用。
